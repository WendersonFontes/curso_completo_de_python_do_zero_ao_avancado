#método .find
item = 'hipoclorito'
pos = item.find('clor') #quando encontra retorna a posição que começar a primeira letra do termo escrito
print(pos)

pos = item.find('flu') #quando não encontra retorna -1
print(pos)
